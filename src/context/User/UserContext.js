import {createContext} from 'react';

// Con el createContext importado lo instanciamos en una constante llamada UserContext
const UserContext = createContext()

// Exportamos la constante para usarla en otro archivo
export default UserContext

